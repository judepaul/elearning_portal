<%= form_with(model: exam, local: true) do |form| %>
  <% if exam.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(exam.errors.count, "error") %> prohibited this exam from being saved:</h2>

      <ul>
      <% exam.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name, %>
    <%= form.text_field :name,, id: :exam_name, %>
  </div>

  <div class="field">
    <%= form.label :type, %>
    <%= form.text_field :type,, id: :exam_type, %>
  </div>

  <div class="field">
    <%= form.label :duration %>
    <%= form.text_field :duration, id: :exam_duration %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
