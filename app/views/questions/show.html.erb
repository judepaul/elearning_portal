<p id="notice"><%= notice %></p>

<p>
  <strong>Text:</strong>
  <%= @question.text %>
</p>

  <% @question.answer_options.each do |option| %>
    <p>
      <%= option.text %>
    </p>
  <% end %>
  <%= link_to 'Back', questions_path %>
|
<%= link_to 'submit', edit_question_path(@question) %>
